<?xml version="1.0" encoding="UTF-8"?>
<thing:thing-descriptions bindingId="bluetooth"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:thing="https://openhab.org/schemas/thing-description/v1.0.0"
	xsi:schemaLocation="https://openhab.org/schemas/thing-description/v1.0.0 https://openhab.org/schemas/thing-description-1.0.0.xsd">

	<thing-type id="generic">
		<supported-bridge-type-refs>
			<bridge-type-ref id="roaming"/>
			<bridge-type-ref id="bluegiga"/>
			<bridge-type-ref id="bluez"/>
		</supported-bridge-type-refs>

		<label>Generic Bluetooth Device</label>
		<description>A generic bluetooth device that supports GATT characteristics</description>

		<config-description>
			<parameter name="address" type="text">
				<label>Address</label>
				<description>Bluetooth address in XX:XX:XX:XX:XX:XX format</description>
			</parameter>
			<parameter name="pollingInterval" type="integer" unit="s">
				<advanced>true</advanced>
				<label>Polling Interval</label>
				<description>The frequency at which readable characteristics refreshed</description>
				<default>30</default>
			</parameter>
			<parameter name="alwaysConnected" type="boolean">
				<label>Connect Automatically</label>
				<description>If enabled, will automatically connect to the device and reconnect if connection is lost.</description>
				<default>true</default>
			</parameter>
			<!--
				connectionSecurity / passkey (per-device LE encryption: none / encrypted / encrypted-with-fallback /
				pin) are intentionally NOT exposed in the UI. The modes are implemented and honoured by the Direct-BT
				adapter (see DirectBTAdapterConstants + DirectBTBluetoothDevice.connectNative), and an existing Thing
				config carrying one of these keys is still respected, but the encrypted modes cannot yet RECONNECT
				reliably (Direct-BT "Defect 2": on bonding the peer distributes an identity address the stack does not
				resolve on reconnect — needs native resolving-list support). Until that is fixed, offering these in the
				UI would invite a half-working choice, so only the default "none" behaviour is surfaced. Re-add the
				<parameter> block below to expose them once bonded reconnect is solid. See
				docs/directbt-security-modes-review.md.
			-->
		</config-description>

	</thing-type>

	<channel-type id="char-unknown">
		<item-type>String</item-type>
		<label>Unknown Bluetooth Characteristic</label>
		<description>The raw value of unknown characteristics are represented with hexadecimal</description>
	</channel-type>

</thing:thing-descriptions>
